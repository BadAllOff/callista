<%= form_for(country) do |f| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this realty country from being saved:</h2>

      <ul>
        <% country.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field form-group">
    <%= f.label :preview_img, 'Картинка над превью текстом' %>
    <%= f.file_field :preview_img %>
  </div>

  <div class="field form-group">
    <%= f.label :country_name %>
    <%= f.text_field :country_name, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= f.label :description %>
    <%= f.cktext_area :description, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
